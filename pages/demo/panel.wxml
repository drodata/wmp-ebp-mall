<import src="../templates/panel.wxml"/>
<view class="page">
  <view class="page-header">
    <view class="page-title">Panel</view>
    <view class="page-desc"></view>
  </view>
  <view class="page-body">
      <template is="panel" data="{{...panel}}"/>
      <template is="panel" data="{{...panelSmall}}"/>
      <template is="panel" data="{{...panelText}}"/>
      <template is="panel" data="{{...panelHybrid}}"/>
  </view>
  <view class="page-footer">
  </view>
</view>
